{
  "name": "netlify-cookie-redirector",
  "version": "1.0.0",
  "description": "URL redirector with header logging",
  "main": "index.js",
  "dependencies": {
    "axios": "^1.6.0"
  },
  "scripts": {
    "build": "echo 'Build step not required for serverless functions'"
  }
}
